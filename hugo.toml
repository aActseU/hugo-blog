baseURL = 'http://13.212.22.21/'
languageCode = 'zh-cn'
title = '我的云端博客'
theme = 'hextra'

# 1. 开启中日韩语言支持 (移到顶部作为全局配置)
hasCJKLanguage = true

# ==========================================
# 核心新增：允许渲染 Markdown 中的 HTML 代码
# ==========================================
[markup.goldmark.renderer]
unsafe = true

[markup.highlight]
noClasses = false

[[menu.main]]
  name = "文章"
  pageRef = "/posts"
  weight = 1

[[menu.main]]
  name = "关于我"
  pageRef = "/about" 
  weight = 2

[params.hextra.search]
  enable = true
  flexsearch = true